# 渡口 v0.2 候选发布清单

交付模式：Vibe Coding 全流程型。来源：需求池 NXT-010B、`docs/prd_v0.2.md`；当前实施与验证事实以 `docs/progress_v0.2.md` 为准。本文不是发布授权或验收通过声明。

## 当前候选

- 本次追加的关页自动退出在 `test@b82e940` 上本地完成，并于 2026-09-14 获得本次提交/推送 `test` 的明确授权；Windows 新 EXE 与 13 项原生关页回归已通过。用户计划 Windows↔Mac 验收，尚无真机通过证据；以最新 progress 与 SHA256 为准。
- 2026-09-14 候选包含关闭补修和功能优先首页：用户已明确授权提交并推送 `test`，不合并 main、不创建 PR/tag/Release。Windows EXE 已重建；最新源状态、校验值及实际运行证据见 `docs/progress_v0.2.md`，远端通过状态必须核对本次新提交。
- 仓库：`E:\codex\LocalFileTransfer`；分支 `test`；修复基线 HEAD `79d4684985836f42494164b20b093db594397d10`，本次三批修复由 test 上包含本记录的提交承载。
- 版本：`0.2.0`；当前为 L2 本地候选，L3 跨设备验收待补证。
- Windows 候选：`dist/dukou-windows-x64.exe`；校验文件 `dist/SHA256SUMS.txt`。只列本轮实际构建并执行 smoke 的产物，不为其他平台补造 checksum。
- 已有源码不等于已发布；当前二进制不在远端 Release。重建会使旧产物身份失效，需重新运行 smoke 并更新校验文件。

## 待执行步骤

1. 追加的关页自动退出已获本次提交/推送授权；推送前核对差异、本地测试与远端 test 一致性，不合并 main、不强推。源码 push 不上传 `dist/` 中的本地 exe。
2. 在远端核对新提交对应的 unit/browser jobs，以及四个平台分别编译、`/healthz`、`/api/runtime`、完整资源与关闭检查。不能沿用旧提交的成功 CI。
3. 登记 Windows/Mac 的 OS、CPU、浏览器及候选哈希；同一局域网执行两个方向的直连、双方批准中转、保存后文件哈希一致、取消/掉线和 TUN 开启场景。Linux 无设备时保留待补证，缩小相应兼容声明。
4. 吞吐基线未执行，不修改分块参数、不声称“更快”。需求池 NXT-009 的 N4 实验数值仍待开始 PERF 批次前确认，不作为已通过的门禁。
5. 对所选发布范围完成实际 acceptance 后，再单独确认 `v0.2.0` tag、GitHub Release 创建/上传与产物范围。发布前重新核验版本、源提交、SHA256、未签名说明及中转安全边界。

## 发布权限与边界

| 动作/发布面 | 当前结论 |
| --- | --- |
| 本地修复、测试、构建及材料 | 本次授权范围内 |
| 提交追加关页功能 / 推送 `test` | 2026-09-14 已获本次明确授权 |
| 远端 CI 新配置运行 | 按本次推送对应提交核验，不提前记为通过 |
| Windows 便携产物 | 本地启动 smoke 通过；跨设备/TUN Beta 验收待补 |
| macOS / Linux 产物 | 新 CI smoke 已配置、尚无本轮执行证据；物理兼容未验收 |
| 创建 `v0.2.0` tag / Release、上传产物 | 暂不执行，待范围验收和单独授权 |
| 修改仓库可见性 | 2026-09-13 已单独授权并改为 public，匿名访问已核验 |
| 安装器、商店发布 | 未在本轮范围内，暂不执行 |

中转若需临时停用，启动前设置 `RELAY_ENABLED=0`；必须验证 runtime/UI/端点均禁用且直连仍可用，不得降级为明文中转。
